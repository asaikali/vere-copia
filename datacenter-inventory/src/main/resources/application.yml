spring:
  datasource:
    url: "jdbc:postgresql://127.0.0.1:25432/central_inventory"
    username: postgres
    password: password
  jpa:
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always


