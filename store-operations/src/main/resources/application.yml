server:
  port: 8080

central_inventory:
  url: "http://localhost:8082"

spring:
  application:
    name: "store-operations"
  datasource:
    url: "jdbc:postgresql://127.0.0.1:25432/store_operations"
    username: postgres
    password: password
  jpa:
    open-in-view: false

management:
  metrics:
    export:
      wavefront:
        api-token: "854c5ef7-fc6a-463b-9706-3ff464abd298"
        uri: "https://try.wavefront.com"
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
